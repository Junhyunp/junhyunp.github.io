# 오늘의 글감 — 2026-08-22 (KST)

> 07:00 자동 갱신 · 취재원 4명 조회 시도 · 훑은 항목 0건 · 후보 0건

## 후보

오늘도 건질 것이 없었다 — 이유는 아래 "취재원 조회 결과" 참고. 후보를 억지로 채우지 않는다.

## 취재원 조회 결과

2026-08-12 부터 오늘까지 **11일 연속** 동일하게, 이 루틴이 도는 환경의 네트워크 egress 프록시가 `sources.yml` 의 활성 취재원 4곳과 arXiv / Hugging Face Papers 도메인을 전부 차단했다. `WebFetch` 호출은 각 도메인에서 즉시 `EGRESS_BLOCKED` 를 반환했다. 프록시 상태(`__agentproxy/status`)를 다시 확인했는데 `recentRelayFailures` 는 비어 있고 프록시 자체(`enabled: true`)는 정상 동작 중이다 — 프록시 장애가 아니라 이 도메인들이 조직 egress 정책에서 의도적으로 차단된 것으로 보인다.

`WebSearch` 는 오늘도 정상 동작했다 (예: Simon Willison 관련 검색은 결과를 반환함). 다만 검색이 짚어준 내용은 8월 13일 Gemini 3.7 Flash 글(이미 발행됨, topics.yml #37)과 8월 9일 글뿐이라 새 글감도 아니었다. 설령 새 글감이 나왔더라도 spec 이 요구하는 "1차 출처를 직접 열어 확인"은 여전히 불가능하다 — 검색 결과가 가리키는 링크 자체를 열 수 없어(같은 도메인이 EGRESS_BLOCKED) 스니펫 이상을 확인할 길이 없다. 검색 스니펫만으로 후보를 세우면 "확인 못한 것을 확인한 것처럼" 적는 셈이 되므로, 오늘도 새 후보를 올리지 않는다.

| 취재원 | 상태 | 최신 항목 |
|---|---|---|
| CHOI (threads) | 실패 — EGRESS_BLOCKED (`www.threads.com` 접근 차단) | — |
| Simon Willison (blog) | 실패 — EGRESS_BLOCKED (`simonwillison.net` 접근 차단) | — |
| Lilian Weng (blog) | 실패 — EGRESS_BLOCKED (`lilianweng.github.io` 접근 차단) | — |
| Ethan Mollick (blog) | 실패 — EGRESS_BLOCKED (`www.oneusefulthing.org` 접근 차단) | — |
| arXiv cs.CL/cs.AI | 실패 — EGRESS_BLOCKED (`arxiv.org` 접근 차단) | — |
| Hugging Face Papers | 실패 — EGRESS_BLOCKED (`huggingface.co` 접근 차단) | — |

## 주의

- **11일 연속**(2026-08-12 ~ 2026-08-22) 취재원 전체가 동일한 방식(`EGRESS_BLOCKED`)으로 막혔다. 프록시는 계속 정상, 도메인 차단만 이어진다.
- `topics.yml` 을 보면 이 자동 아침 루틴이 막혀 있는 동안에도 로컬 세션(사람이 직접 붙어 WebFetch 를 쓴 세션)으로는 취재·검증·발행이 계속 이뤄지고 있다 (#37, #38, #40, #41, #42 모두 발행 완료). 즉 콘텐츠 공급 자체가 끊긴 건 아니고, **이 자동 루틴만 11일째 기여가 없다.**
- `sources.yml` 이 적어둔 대로, 고치려면 claude.ai/code 에서 Network access `Full` 인 환경을 새로 만들어 이 루틴을 그쪽으로 옮겨야 한다. 열흘을 넘겨 반복 확인만 하고 있는 상태라, 이번에는 별도로 사용자에게 알린다.
